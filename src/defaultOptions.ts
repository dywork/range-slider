const defaultOptions = {
  currentValue: 0,
  range: {
    min: 0,
    max: 100,
  },
  isThumb: true,
  isRuler: false,
  step: 0,
  decimal: 2,
  orientation: 'horizontal',
};

export default defaultOptions;
